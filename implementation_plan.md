# План реализации: Перемещение cross-sell товаров

## Постановка проблемы

**Текущее поведение:**
- Товары "А вдруг пригодится?" отображаются внизу категории после всех линеек
- Показываются с плашкой/заголовком "А вдруг пригодится?"
- Блок находится в строках 191-219 в `HomeView.vue`

**Требуемое поведение:**
- Товары должны отображаться **первыми** (сверху, перед всеми линейками)
- Без плашки "А вдруг пригодится?" - только сами карточки товаров
- Количество товаров = количеству выбранных в модалке админки

## Текущая архитектура

### Frontend (HomeView.vue)

**Структура отображения (строки 189-221):**
```
1. Группы/линейки (если есть)
2. Специальный showcase для liquids (если включен)
3. [ТЕКУЩЕЕ] Блок cross-sell внизу (строки 191-219)
4. Список товаров (если нужен)
```

**Ключевые элементы:**
- `crossSellItems` computed (строка 564) - получает товары из стора
- Компонент использует класс `.cross-sell-card` (стили строки 1202-1216)
- Секция `<section v-if="crossSellItems.length" class="mt-12">` с заголовком

### Backend (routes/public.js)

**API endpoint:** `/api/cross-sells` (строки 309-383)
- Получает товары из таблицы `category_cross_sells`
- Сортирует по полю `[order]`
- Возвращает массив товаров с полной информацией

### База данных (db.js)

**Таблица:** `category_cross_sells` (строки 91-97)
- `categoryId` - привязка к категории
- `productId` - ID товара
- `[order]` - порядок отображения
- Уникальная пара (categoryId, productId)

## Предлагаемые изменения

### 1. Frontend: HomeView.vue

**Изменение А: Переместить блок cross-sell**
- **Текущее расположение:** строки 191-219 (после групп и showcase)
- **Новое расположение:** сразу после выбора категории, перед группами
- **Удалить:** секцию с заголовком "А вдруг пригодится?" (строки 191-194)
- **Оставить:** только сетку карточек товаров (строки 195-218)

**Конкретный план:**
1. Вырезать секцию строк 191-219
2. Вставить **перед** строкой 105 (перед `<div v-else-if="groupCards.length">`)
3. Убрать элемент с классом `section-header-navalivay` и заголовком
4. Изменить обертку с `<section>` на `<div>` для единообразия
5. Убрать отступ `mt-12`, заменить на `mb-6` или `mb-8`

**Результат:**
```
Категория выбрана
↓
[Cross-sell товары БЕЗ заголовка] ← НОВОЕ место
↓
Группы/линейки
↓
Showcase (если есть)
↓
Список товаров
```

### 2. CSS стили

**Проверить совместимость:**
- Класс `.cross-sell-card` (строки 1202-1214) уже существует
- Класс `.cross-sell-media` (строка 1216) используется
- Класс `.cross-sell-body` (строка 1276) используется
- Класс `.cross-sell-title` (строка 1284) используется
- Класс `.cross-sell-price` (строка 1306) используется
- **Изменений в CSS не требуется**

### 3. Backend и БД

**Изменений не требуется:**
- API уже возвращает товары в правильном порядке
- Логика выбора в админке работает корректно
- Таблица БД структурирована правильно

## Файлы для изменения

1. **frontend/src/views/HomeView.vue** - единственный файл, который нужно изменить
   - Переместить блок cross-sell (строки 191-219)
   - Убрать заголовок секции
   - Скорректировать отступы

## Риски и проверки

1. **Визуальная совместимость** - убедиться, что товары корректно отображаются вверху
2. **Адаптивность** - проверить на мобильных устройствах
3. **Пустое состояние** - убедиться, что при `crossSellItems.length === 0` ничего лишнего не показывается
4. **Liquid showcase** - проверить, что для режима `liquid` товары отображаются корректно

## Порядок внедрения

1. ✅ Создать резервную копию `HomeView.vue`
2. ✅ Переместить секцию cross-sell в начало (после выбора категории)
3. ✅ Удалить заголовок "А вдруг пригодится?"
4. ✅ Применить дизайн LiquidLineCard к cross-sell товарам
5. ✅ Добавить функционал корзины (кнопки +/-, управление количеством)
6. ✅ Проверить отступы и визуальное выравнивание
7. 🔲 Протестировать в браузере
8. 🔲 Проверить адаптивность

## Реализованные изменения

### Изменения в разметке:
- Cross-sell товары перемещены в начало (строки 82-147)
- Применен стиль `liquid-line-card-single` вместо `product-card`
- Добавлена горизонтальная карточка с картинкой слева
- Добавлены кнопки управления количеством товара в корзине
- Убран клик по всей карточке, оставлен только на info блоке

### Добавлены функции (строки 728-804):
- `getCrossSellQuantity()` - получение количества товара в корзине
- `canAddCrossSell()` - проверка возможности добавления
- `isCrossSellAtStockLimit()` - проверка лимита остатков
- `getCrossSellStockMessage()` - сообщение о наличии
- `addCrossSellToCart()` - добавление в корзину
- `incrementCrossSellQuantity()` - увеличение количества
- `decrementCrossSellQuantity()` - уменьшение количества

### Добавлены стили (строки 1891-2180):
- `.liquid-line-card-single` - основная карточка товара
- `.liquid-line-single-header` - header с контентом
- `.liquid-line-single-main` - левая часть с изображением и названием
- `.liquid-line-single-side` - правая часть с ценой и кнопками
- `.liquid-flavor-add` - кнопка добавления в корзину
- `.liquid-flavor-quantity` - блок управления количеством
- `.flavor-qty-btn` - кнопки +/-
- Адаптивные медиа-запросы для всех разрешений
